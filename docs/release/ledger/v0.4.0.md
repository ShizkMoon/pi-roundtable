# v0.4.0 release ledger

Ledger opened: 2026-08-04

Release policy simplified: 2026-08-05

Release state: published and verified on 2026-08-05 (Asia/Shanghai)

This is a personal-project release ledger. `verified` is reserved for evidence
bound to the final main commit and candidate bytes. Authenticode, a disposable
clean VM, and a three-DPI visual matrix are optional and do not block v0.4.0.

| Evidence | State | Current record |
| --- | --- | --- |
| Protected-main commit and CI | verified | Release tag `v0.4.0` points to `fc6990609566af85d7789b67401f3f5996dae0a8`; main push CI run `30934784177` passed C++, TypeScript, and Windows. |
| Full release build and default ICE | verified | ReleaseCandidate run `20260804-175609-915-2b69a598` completed with `releaseEligible: true`; report SHA-256 `66FCC879F16D6BFFC0ADAAD69AAC5743276ABC382C5721D67548B975883DEC21`. |
| Full-payload isolated MSI lifecycle | verified | Evidence `12c43796-fa27-469f-9315-c506fca6673d` verified 10,873 installed files plus runtime imports, two launches, two repairs, major upgrade, blocked downgrade, uninstall, and no QA residue. |
| Version, metadata and release materials | verified | The five canonical assets are bound to version `0.4.0` and the release commit. Public MSI: 148,848,943 bytes, SHA-256 `D9399C65596BAF368AB790122992A2C0C104771764CCA7CBB02B49C4A711CC4F`. |
| Draft/public re-download | verified | Publication evidence `322ac76f-519f-423b-914a-54e88e439d41` re-downloaded and verified the draft, pre-publication, and public assets; report path `out/e2e/release-publication/v0.4.0/run-377cfef87a37/release-publication-report.json`. |
| Stable updater manifest | verified | The ECDSA-signed stable manifest is bound to the verified public v0.4.0 MSI and declares `authenticodeRequired: false`. |
| Authenticode | optional; not configured | The unsigned personal MSI may trigger Windows publisher warnings. If later enabled, trust and timestamp checks remain fail-closed. |
| Clean production VM / 96,144,192 DPI / real provider | optional; pending | Useful diagnostics, not v0.4.0 release requirements and not claimed as verified. |

## Required commands

```powershell
npm run quality:fast
npm run quality:windows
npm run quality:release-candidate
pwsh -File .\scripts\publish-windows-release.ps1 `
  -ReleaseCandidateReportPath <quality-gate-report.json> `
  -ReleaseNotesPath .\docs\release\v0.4.0.md `
  -Publish
```

The ReleaseCandidate command must report `releaseEligible: true`. A dirty
worktree, failed/skipped tests, skipped default ICE validation, mismatched
metadata, an incomplete full-payload lifecycle, or changed release bytes still
blocks publication.
