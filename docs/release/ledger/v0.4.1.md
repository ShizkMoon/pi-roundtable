# v0.4.1 release ledger

Ledger opened: 2026-08-05

Release state: published and verified on 2026-08-05 (Asia/Shanghai)

This is a personal-project release ledger. `verified` is reserved for evidence
bound to the final main commit and candidate bytes. Authenticode, a disposable
clean VM, and a three-DPI visual matrix are optional and do not block v0.4.1.

| Evidence | State | Current record |
| --- | --- | --- |
| Protected-main commit and CI | verified | Release tag `v0.4.1` points to `9789ba7ae7dde0ced7f8cf361898b3c04cfa7f68`; main push CI run `31005024312` passed C++, TypeScript, and Windows. |
| Full release build and default ICE | verified | ReleaseCandidate run `20260805-123807-761-8c1bad4b` completed with `releaseEligible: true`; report SHA-256 `D0F47096FF3302857B7B8F7DECC85CB4656F5AF08EDA6428FAAEA99C59C33AB4`. |
| Full-payload isolated MSI lifecycle | verified | Evidence `0ac85d00-ae0b-47a5-a874-49ebd09c45b9` verified 10,874 installed files plus runtime imports, two launches, two repairs, major upgrade, blocked downgrade, uninstall, and no QA residue. |
| Version, metadata and release materials | verified | The five canonical assets are bound to version `0.4.1` and the release commit. Public MSI: 148,869,540 bytes, SHA-256 `FEFBD3C9AD7856ED040F6C8FC5E80D3A9FE54AD7838BFCAE7EADA6B8F020601F`. |
| Draft/public re-download | verified | Publication evidence `c7b0a3de-86ec-4055-9c5d-fc871a62296e` re-downloaded and verified the draft, pre-publication, and public assets; report path `out/e2e/release-publication/v0.4.1/run-ccf2291b72ae/release-publication-report.json`. |
| Stable updater manifest | verified | The ECDSA-signed stable manifest is bound to the verified public v0.4.1 MSI and declares `authenticodeRequired: false`. |
| Authenticode | optional; not configured | The unsigned personal MSI may trigger Windows publisher warnings. If later enabled, trust and timestamp checks remain fail-closed. |
| Clean production VM / 96,144,192 DPI / real provider | optional; pending | Useful diagnostics, not v0.4.1 release requirements and not claimed as verified. |

## Required commands

```powershell
npm run quality:fast
npm run quality:windows
npm run quality:release-candidate
pwsh -File .\scripts\publish-windows-release.ps1 `
  -ReleaseCandidateReportPath <quality-gate-report.json> `
  -ReleaseNotesPath .\docs\release\v0.4.1.md `
  -Publish
```

The ReleaseCandidate command must report `releaseEligible: true`. A dirty
worktree, failed or skipped tests, skipped default ICE validation, mismatched
metadata, an incomplete full-payload lifecycle, or changed release bytes still
blocks publication.
